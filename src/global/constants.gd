extends Node2D

enum DAMAGE_TYPE_ENUM {
	PHYSIC,
	VOID,
	NATURE,
	LIGHTING,
	FIRE
}

enum CAST_TYPE_ENUM {
	BASE,
	CRITICAL,
	HEAL
}

enum WEAPON_TYPES {
	SWORD,
	BOW,
}

enum ABILITY_TYPE_ENUM {
	ACTIVE,
	PASSIVE,
	MAINTAIN
}

enum DOT_TYPE_ENUM {
	BLEED,
	POISON,
	BURN
}

enum ITEM_TYPE_ENUM {
	WEAPON,
	ARTIFACT
}
