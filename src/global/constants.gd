extends Node2D

enum DAMAGE_TYPE_ENUM {
	PHYSIC,
	VOID,
	NATURE,
	LIGHTING,
	FIRE,
}

enum EFFECT_TAG_ENUM {
	BLEED,
	POISON,
	BURN,
}

enum SCALING_TAG_ENUM {
	PROJECTILE,
	DAMAGE_OVER_TIME,
	AOE,
	AURA,
	ATTACK,
	SPELL
}

enum DAMAGE_WEIGHT_ENUM {
	BASE,
	CRITICAL,
}

enum WEAPON_TYPES {
	ALL,
	SWORD,
	BOW,
	HAMMER,
	STAFF
}

enum ABILITY_TYPE_ENUM {
	ACTIVE,
	PASSIVE,
	MAINTAIN,
	TOGLE
}

enum ITEM_TYPE_ENUM {
	WEAPON,
	ARTIFACT,
	ABILITY_SCROLL
}

enum SCROLL_RARITY_ENUM {
	NORMAL,
	RARE,
	MAGIC,
	LEGENDARY
}
